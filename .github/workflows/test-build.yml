name: Test Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python 3.14.6
      uses: actions/setup-python@v5
      with:
        python-version: '3.14.6'

    - name: Install PyInstaller
      run: python -m pip install pyinstaller pandas sys subprocess importlib os glob datetime zipfile ElementTree tkinter

    - name: Build executable
      run: pyinstaller --onefile word-metadata-extractor.py

    - name: Upload test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: test-build-${{ matrix.os }}
        path: dist/*